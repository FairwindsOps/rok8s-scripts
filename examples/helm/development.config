NAMESPACE='development'

# deploy the same chart twice with different release name and values
HELM_CHARTS=('charts/example' 'charts/example')
HELM_RELEASE_NAMES=('example' 'example-alt')
HELM_VALUES=('values/development' 'values/development-alt')
HELM_TIMEOUTS=('400', '600')
